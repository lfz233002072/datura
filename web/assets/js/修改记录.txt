
2014.12.12
	文件：Scripts/jquery.validate.js 
	修改内容:
		1、修改highlight、unhighlight两个方法。表示验证时输入框的父div会添加errorClass 样式类。控制显示样式。 具体代码如下
			
			var parentElement = $(element).parent("div");
			if (parentElement.size() != 0) { 
			    $(parentElement).addClass(errorClass).removeClass(validClass);
		    }
		2、修改jQuery.validator的默认选择errorClass,validClass的值。虽然可以通过配置jQuery.validator.setDefaults设置。但未使用
	
	文件：Scripts/jquery.unobtrusive-ajax.js 
	修改内容:
		1、修改默认显示样式(验证时)

	